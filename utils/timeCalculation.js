const daysToSeconds = (days) => {
    return Math.round(days * 86400);
};

const calculateExpireDate = (startDate, duration) => {
    return 
};

module.exports = { daysToSeconds, calculateExpireDate }